# learningrepo
samiksha shejwal this is mee!!!

git --version
git clone
ls:for list
ls-h :for hidden files
git status
git add file_name :  for file name
git add . :adds all files
git commit -m "meaning ful line"
cd .. "back to org dc"
mkdir "new dc"
git init
git branch -M branch_name
git push -u origin main (when working on the project for long and will need in future like short cut next time use git push)
git checkout -b nameof_newbranch
git checkout nameof_file :to go form the current branch to the next one 
git checkout -d file_name  :to delete the branch(can not delete the branch we are alredy in )
git push origin branch_name :to push this new creayed branch
git diff branch_main: other branch name to compare with current branch
git merge branch_name: to merge two branches(q)
git pull origin main
<br>
hii
